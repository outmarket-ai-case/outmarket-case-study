# Lint fixture: what the pipeline renders for AWS/dev. Kept in ci/ so
# `helm lint --values ci/*.yaml` covers every provider branch in the chart.
platform:
  cloud: aws
  environment: dev
  region: eu-west-1
  accountId: "123456789012"
  cluster:
    name: idea-board-dev
    ingressClass: alb
    storageClass: gp2
  registry:
    host: 123456789012.dkr.ecr.eu-west-1.amazonaws.com
    repositoryPrefix: idea-board
  database:
    host: idea-board-dev.abc.eu-west-1.rds.amazonaws.com
    port: 5432
    name: ideas
    username: ideas_app
    secretRef: arn:aws:secretsmanager:eu-west-1:123456789012:secret:idea-board-dev/database-AbCdEf
    secretBackend: secretsmanager
  serviceAccountAnnotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::123456789012:role/idea-board-dev-app
  appHostname: ""
image:
  tag: "sha-abc1234"
