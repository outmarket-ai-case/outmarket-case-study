# python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/

# node
node_modules/
dist/
.vite/

# env / secrets -- never commit these
.env
.env.*
!.env.example
*.pem
*.tfvars.secret
kubeconfig*

# terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
crash.log
tfplan
tfplan.json

# ai artifacts
.aiops/
*.log

# Demo artefacts: the mp4 is committed as a deliverable; the intermediate
# webm and extracted frames are not.
demo/out/*.webm
demo/out/*.png
demo/out/screen.mov

# Demo artefacts: the mp4 is committed as a deliverable; the intermediate
# webm and extracted frames are not.
demo/out/*.webm
demo/out/*.png
demo/out/screen.mov

# TypeScript incremental build cache
frontend/tsconfig.tsbuildinfo
